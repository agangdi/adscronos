# Frontend API Host
# The backend server URL for API calls
VITE_API_HOST=http://localhost:8000

# Base URL for built assets (used by build-all.mts)
# The URL where the built JS/CSS files will be served from
CHATGPT_APPS_BUILD_BASE_URL=http://localhost:4444
